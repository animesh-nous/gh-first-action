name: First workflow
on: workflow_dispatch
jobs: 
  # any job name. (50 minutes)
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello Animesh"
      - name: Print goodbye
        run: echo "bye Animesh"
